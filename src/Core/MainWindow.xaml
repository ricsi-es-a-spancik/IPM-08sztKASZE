<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="Core.MainWindow"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="File" Height="25" Command="{Binding LoadModuleCommand}">
                <TextBlock><Run Text="Modul Betöltése"/></TextBlock>
            </MenuItem>
        </Menu>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding GameList}">
            <ItemsControl.Background>
                <SolidColorBrush>
                    <SolidColorBrush.Color>
                        <Color R="200" G="200" B="200" A="255"/>
                    </SolidColorBrush.Color>
                </SolidColorBrush>
            </ItemsControl.Background>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding GameNum}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                    <Button Command="{Binding LaunchGameCommand}" CommandParameter="{Binding Id}" Content="{Binding GameName}">
                        <Button.Background>
                            <SolidColorBrush Color="Gray"/>
                        </Button.Background>
                    </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock DataContext="{Binding Status}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
