<Window x:Class="Core.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="120" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Grid.ColumnSpan="2" Grid.Row="0" HorizontalAlignment="Center" Content="Input Your name!"/>
        <TextBox Width="100" Height="25" Name="Input" Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Center"></TextBox>
        <Button Grid.Row="2" Grid.Column="0" Content="OK" Command="{Binding Ok}"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Cancel" Command="{Binding Cancel}"/>

    </Grid>
</Window>
